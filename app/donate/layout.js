export const metadata = {
  title: "Donate & Support - NoQuantumBS",
  description: "Support our mission to cut through quantum computing hype",
};

export default function DonateLayout({ children }) {
  return children;
}
